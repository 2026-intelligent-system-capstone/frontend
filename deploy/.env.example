FRONTEND_IMAGE_TAG=2026-05-07-1
BACKEND_IMAGE_TAG=2026-05-07-1
POSTGRES_DB=dialearn
POSTGRES_PASSWORD=change-me-strong-password

# Public origin served by the existing Caddy container.
# Use https://example.com if Caddy routes /api to the backend on the same domain.
# Use https://api.example.com if Caddy exposes backend on a separate API domain.
NEXT_PUBLIC_API_BASE_URL=https://dialearn.presso.ac
