# Public origin served by the existing Caddy container.
# Same-domain routing example: https://example.com/api -> backend
# Separate API domain example: https://api.example.com -> backend
NEXT_PUBLIC_API_BASE_URL=https://dialearn.presso.ac
